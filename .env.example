# Database Configuration
DB_NAME=CocherasUTN
DB_HOST=localhost
DB_PORT=3306
DBUSER=cocheras
DBPASS=cocheras_password
DB_ROOT_PASSWORD=root
# Alternative: Use DATABASE_URL for production (overrides individual DB settings)
# DATABASE_URL=mysql://user:password@host:port/database

# Application Configuration
NODE_ENV=production
PORT=3000

# JWT Configuration
JWT_SECRET=your_jwt_secret_here_change_in_production

# CORS Configuration (for production, use your frontend domain)
CORS_ORIGIN=https://your-frontend-domain.com
